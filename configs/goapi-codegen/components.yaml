# yaml-language-server: $schema=https://raw.githubusercontent.com/oapi-codegen/oapi-codegen/HEAD/configuration-schema.json
package: components
generate:
  models: true
output: handlers/components/gen.go
output-options:
  # to make sure that all types are generated
  skip-prune: true
